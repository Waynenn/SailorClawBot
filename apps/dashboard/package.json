{
  "name": "@sailorclawbot/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  }
}
