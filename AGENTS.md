# SailorClawBot — Agent Instructions

This repository keeps a **single source of truth** for AI-agent instructions
in [CLAUDE.md](./CLAUDE.md).

Read **[CLAUDE.md](./CLAUDE.md)** — it covers the monorepo layout, package
dependency order, current phase status, key files, the Prisma schema models,
architectural patterns, commands, and code style.

> Do not duplicate instructions here. Keeping this file a pointer prevents the
> drift that previously left `AGENTS.md` frozen several phases behind `CLAUDE.md`.
